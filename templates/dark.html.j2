<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ user.name }} - Currículo</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #e0e0e0;
            background: #121212;
            padding: 20px;
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
            background: #1e1e1e;
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 0 30px rgba(0,0,0,0.5);
            border: 1px solid #333;
        }
        
        .header {
            text-align: center;
            border-bottom: 3px solid #bb86fc;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }
        
        .header h1 {
            font-size: 2.5em;
            color: #bb86fc;
            margin-bottom: 10px;
            text-shadow: 0 0 10px rgba(187, 134, 252, 0.3);
        }
        
        .contact-info {
            display: flex;
            justify-content: center;
            gap: 20px;
            flex-wrap: wrap;
            margin-top: 15px;
        }
        
        .contact-item {
            display: flex;
            align-items: center;
            gap: 5px;
            color: #b0b0b0;
        }
        
        .section {
            margin-bottom: 30px;
        }
        
        .section h2 {
            color: #bb86fc;
            border-bottom: 2px solid #333;
            padding-bottom: 10px;
            margin-bottom: 20px;
            font-size: 1.5em;
        }
        
        .professional-summary {
            background: #2a2a2a;
            padding: 20px;
            border-radius: 8px;
            border-left: 4px solid #bb86fc;
            font-style: italic;
        }
        
        .skills-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 10px;
        }
        
        .skill-item {
            background: linear-gradient(135deg, #bb86fc, #9c4dcc);
            padding: 8px 12px;
            border-radius: 20px;
            text-align: center;
            font-weight: 500;
            color: white;
        }
        
        .project {
            background: #2a2a2a;
            border: 1px solid #444;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 20px;
            transition: all 0.3s;
        }
        
        .project:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(187, 134, 252, 0.2);
            border-color: #bb86fc;
        }
        
        .project h3 {
            color: #bb86fc;
            margin-bottom: 10px;
        }
        
        .project-meta {
            display: flex;
            gap: 15px;
            margin: 10px 0;
            font-size: 0.9em;
            color: #b0b0b0;
        }
        
        .project-link {
            display: inline-block;
            background: linear-gradient(135deg, #bb86fc, #9c4dcc);
            color: white;
            padding: 8px 16px;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 10px;
            transition: all 0.2s;
        }
        
        .project-link:hover {
            transform: scale(1.05);
            box-shadow: 0 4px 15px rgba(187, 134, 252, 0.4);
        }
        
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 20px;
            margin-top: 20px;
        }
        
        .stat-card {
            background: linear-gradient(135deg, #333, #444);
            color: white;
            padding: 20px;
            border-radius: 8px;
            text-align: center;
            border: 1px solid #555;
        }
        
        .stat-number {
            font-size: 2em;
            font-weight: bold;
            display: block;
            color: #bb86fc;
        }
        
        .languages-list {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            margin-top: 15px;
        }
        
        .language-tag {
            background: linear-gradient(135deg, #03dac6, #018786);
            color: white;
            padding: 5px 10px;
            border-radius: 15px;
            font-size: 0.9em;
        }
        
        .gamification {
            background: linear-gradient(135deg, #2a2a2a, #333);
            padding: 25px;
            border-radius: 10px;
            text-align: center;
            border: 2px solid #bb86fc;
        }
        
        .level-badge {
            font-size: 1.5em;
            margin: 10px 0;
            color: #bb86fc;
        }
        
        .badges-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-top: 20px;
        }
        
        .badge-item {
            background: #444;
            padding: 15px;
            border-radius: 8px;
            text-align: center;
            border: 1px solid #555;
        }
        
        .footer {
            text-align: center;
            margin-top: 40px;
            padding-top: 20px;
            border-top: 1px solid #444;
            color: #b0b0b0;
            font-style: italic;
        }
        
        @media print {
            body { background: white; color: black; }
            .container { background: white; color: black; box-shadow: none; }
            .header h1 { color: #333; }
            .section h2 { color: #333; }
        }
    </style>
</head>
<body>
    <div class="container">
        <header class="header">
            <h1>{{ user.name }}</h1>
            <div class="contact-info">
                {% if user.email %}<div class="contact-item">📧 {{ user.email }}</div>{% endif %}
                {% if user.location %}<div class="contact-item">📍 {{ user.location }}</div>{% endif %}
                {% if user.blog %}<div class="contact-item">🌐 {{ user.blog }}</div>{% endif %}
                {% if user.company %}<div class="contact-item">🏢 {{ user.company }}</div>{% endif %}
            </div>
        </header>

        <section class="section">
            <h2>👨‍💻 Perfil Profissional</h2>
            <div class="professional-summary">
                {{ professional_summary }}
            </div>
        </section>

        <section class="section">
            <h2>🛠️ Habilidades Técnicas</h2>
            <div class="skills-grid">
                {% set all_skills = [] %}
                {% for repo in repositories %}
                  {% for skill in repo.llm_skills %}
                    {% if skill not in all_skills %}
                      {% set _ = all_skills.append(skill) %}
                    {% endif %}
                  {% endfor %}
                {% endfor %}
                {% for skill in all_skills[:15] %}
                <div class="skill-item">{{ skill }}</div>
                {% endfor %}
            </div>
        </section>

        <section class="section">
            <h2>🚀 Projetos em Destaque</h2>
            {% for repo in repositories[:6] %}
            <div class="project">
                <h3>{{ repo.llm_title }}</h3>
                {% if repo.llm_description %}
                <p>{{ repo.llm_description }}</p>
                {% endif %}
                
                <div class="project-meta">
                    <span><strong>Tecnologias:</strong> {{ repo.llm_skills | join(', ') }}</span>
                    {% if repo.stars > 0 %}<span>⭐ {{ repo.stars }}</span>{% endif %}
                    {% if repo.forks > 0 %}<span>🍴 {{ repo.forks }}</span>{% endif %}
                </div>
                
                {% if repo.llm_achievements %}
                <div>
                    <strong>Conquistas:</strong>
                    <ul>
                    {% for achievement in repo.llm_achievements %}
                        <li>{{ achievement }}</li>
                    {% endfor %}
                    </ul>
                </div>
                {% endif %}
                
                <a href="{{ repo.url }}" class="project-link" target="_blank">Ver Projeto</a>
            </div>
            {% endfor %}
        </section>

        <section class="section">
            <h2>📊 Estatísticas GitHub</h2>
            <div class="stats-grid">
                <div class="stat-card">
                    <span class="stat-number">{{ stats.total_repos }}</span>
                    <span>Repositórios</span>
                </div>
                <div class="stat-card">
                    <span class="stat-number">{{ stats.total_stars }}</span>
                    <span>Total Stars</span>
                </div>
                <div class="stat-card">
                    <span class="stat-number">{{ stats.total_forks }}</span>
                    <span>Total Forks</span>
                </div>
                <div class="stat-card">
                    <span class="stat-number">{{ stats.followers }}</span>
                    <span>Seguidores</span>
                </div>
            </div>
            
            <h3>💻 Linguagens Mais Utilizadas</h3>
            <div class="languages-list">
                {% for language, count in stats.languages.items() %}
                <div class="language-tag">{{ language }} ({{ count }})</div>
                {% endfor %}
            </div>
        </section>

        {% if gamified %}
        <section class="section">
            <h2>🎮 Conquistas de Desenvolvedor</h2>
            <div class="gamification">
                {% set total_points = (stats.total_stars * 10) + (stats.total_forks * 5) + (stats.total_repos * 2) + (stats.languages|length * 15) %}
                
                <div class="level-badge">
                    🎯 <strong>{{ total_points }} pontos</strong>
                </div>
                
                <div class="level-badge">
                    {% if total_points >= 1000 %}
                    🏅 🚀 Ninja Master
                    {% elif total_points >= 500 %}
                    🏅 ⚡ Code Warrior  
                    {% elif total_points >= 200 %}
                    🏅 💻 Script Kiddie
                    {% else %}
                    🏅 🌱 Padawan Developer
                    {% endif %}
                </div>
                
                <div class="badges-grid">
                    <div class="badge-item">
                        <div>🌟 <strong>Star Collector</strong></div>
                        <div>{{ stats.total_stars }} stars</div>
                    </div>
                    <div class="badge-item">
                        <div>🍴 <strong>Fork Master</strong></div>
                        <div>{{ stats.total_forks }} forks</div>
                    </div>
                    <div class="badge-item">
                        <div>📚 <strong>Polyglot</strong></div>
                        <div>{{ stats.languages|length }} linguagens</div>
                    </div>
                    <div class="badge-item">
                        <div>🚀 <strong>Project Creator</strong></div>
                        <div>{{ stats.total_repos }} repositórios</div>
                    </div>
                </div>
            </div>
        </section>
        {% endif %}

        <footer class="footer">
            <p>Currículo gerado automaticamente pelo GitHub ATS Resume Generator 🚀</p>
        </footer>
    </div>
</body>
</html>
